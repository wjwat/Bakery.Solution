<h1>Treats</h1>

@if (@Model.Count == 0)
{
  <h3>No Treats have been created yet!</h3>
}
else
{
  <hr />
  @await Html.PartialAsync("_Tables/_Treats")
  <hr />
}

@if (User.Identity.IsAuthenticated)
{
  @await Html.PartialAsync("_Buttons/_AddFlavor")
  @await Html.PartialAsync("_Buttons/_AddTreat")
}
